# Synditech RealNext - Visual Architecture & Data Flow

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          EXTERNAL SERVICES                              │
│  ┌──────────┐  ┌──────────────┐  ┌───────┐  ┌──────────┐  ┌──────────┐ │
│  │ Google   │  │  Razorpay    │  │ n8n   │  │Facebook  │  │ Twilio   │ │
│  │ OAuth    │  │  Payments    │  │ WF    │  │ Webhooks │  │  SMS     │ │
│  └──────────┘  └──────────────┘  └───────┘  └──────────┘  └──────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
                                   ↑
                    ┌──────────────┼──────────────┐
                    ↓              ↓              ↓
        ┌──────────────────┐  ┌──────────────────┐
        │   FRONTEND       │  │    BACKEND       │
        │   (Next.js 14)   │  │   (Express.js)   │
        ├──────────────────┤  ├──────────────────┤
        │ • Auth Pages     │  │ Auth Routes      │
        │ • Dashboard      │→→→ • Client Routes   │
        │ • Campaigns      │  │ • Admin Routes   │
        │ • Analytics      │  │ • Subscription   │
        │ • Team Mgmt      │  │ • Webhooks       │
        │ • Settings       │  │ • External APIs  │
        └──────────────────┘  └──────────────────┘
                         ↓        ↓
                  ┌──────────────────────┐
                  │    MongoDB Atlas     │
                  │  (External Instance) │
                  │  72.61.248.175:5443  │
                  └──────────────────────┘
```

## 👤 User Authentication Flow

```
User Login Request
      ↓
┌─────────────────────────────────────────────┐
│ 1. Receive email + password                 │
└─────────────────────────────────────────────┘
                   ↓
         ┌─────────────────────┐
         │ 2. Lookup User in DB│
         └─────────────────────┘
                   ↓
     ┌──────────────────────────────┐
     │3. Verify password hash       │
     │   (bcrypt compare)           │
     └──────────────────────────────┘
                   ↓
         ┌─────────────────────┐
         │ Success? YES/NO     │
         └─────────────────────┘
            ↙         ↘
         NO           YES
         ↓            ↓
    Return       ┌──────────────────────┐
    401 Error    │ 4. Get User Context  │
                 │    (ClientUser data) │
                 └──────────────────────┘
                           ↓
                 ┌──────────────────────┐
                 │ 5. Build JWT Payload │
                 │    ├─ user_id        │
                 │    ├─ client_id      │
                 │    ├─ is_super_admin │
                 │    └─ exp: 15m       │
                 └──────────────────────┘
                           ↓
                 ┌──────────────────────┐
                 │ 6. Generate Tokens   │
                 │    ├─ Access Token   │
                 │    └─ Refresh Token  │
                 └──────────────────────┘
                           ↓
                 ┌──────────────────────┐
                 │ 7. Update last_login │
                 │    and save User     │
                 └──────────────────────┘
                           ↓
                 ┌──────────────────────┐
                 │ 8. Log Auth Event    │
                 │    (Audit)           │
                 └──────────────────────┘
                           ↓
                    Return Tokens
                    + User Info
```

## 🔐 Request Authorization Flow

```
Incoming Request with JWT Token
      ↓
┌────────────────────────────────────────┐
│ 1. Extract Bearer token from header    │
└────────────────────────────────────────┘
      ↓
┌────────────────────────────────────────┐
│ 2. Verify JWT signature & expiry       │
└────────────────────────────────────────┘
      ↓
      ├─ EXPIRED? → Return 401
      │
      └─ VALID? Continue...
             ↓
┌────────────────────────────────────────┐
│ 3. Load User from Database             │
│    WHERE _id = decoded.sub             │
└────────────────────────────────────────┘
      ↓
      ├─ NOT FOUND? → Return 401
      │
      ├─ SUSPENDED? → Return 401
      │
      └─ ACTIVE? Continue...
             ↓
┌────────────────────────────────────────┐
│ 4. Attach User to req.user             │
│    ├─ is_super_admin                   │
│    ├─ email                            │
│    └─ _id                              │
└────────────────────────────────────────┘
      ↓
      ├─ IS SUPER ADMIN? → Bypass all checks, Next
      │
      └─ Regular User? Continue...
             ↓
┌────────────────────────────────────────┐
│ 5. Load ClientUser relationship        │
│    WHERE user_id & client_id           │
└────────────────────────────────────────┘
      ↓
      ├─ NOT FOUND? → Return 403 Forbidden
      │
      └─ FOUND? Continue...
             ↓
┌────────────────────────────────────────┐
│ 6. Load Client Organization            │
│    ├─ status: active/inactive          │
│    ├─ settings                         │
│    └─ Attach req.client                │
└────────────────────────────────────────┘
      ↓
      ├─ NOT ACTIVE? → Return 403
      │
      └─ ACTIVE? Continue...
             ↓
┌────────────────────────────────────────┐
│ 7. Load Subscription & Plan Features   │
│    WHERE client_id = decoded.client_id │
│    AND status IN ['trial', 'active']   │
└────────────────────────────────────────┘
      ↓
┌────────────────────────────────────────┐
│ 8. Build req.features map              │
│    ├─ leads: true/false                │
│    ├─ campaigns: true/false            │
│    ├─ meta_ads: true/false             │
│    └─ ...all plan features             │
└────────────────────────────────────────┘
      ↓
┌────────────────────────────────────────┐
│ 9. Build req.featureLimits map         │
│    ├─ leads.max_contacts: 1000         │
│    ├─ campaigns.max: 50                │
│    └─ ...all plan limits               │
└────────────────────────────────────────┘
      ↓
┌────────────────────────────────────────┐
│ 10. Attach user role & permissions     │
│     req.user.client_role = 'admin'     │
│     req.user.permissions = [...]       │
└────────────────────────────────────────┘
      ↓
    AUTHENTICATED & AUTHORIZED
    Ready for route handler
```

## 💳 Subscription Management Flow

```
SUBSCRIPTION LIFECYCLE

┌─────────────────────────────────────┐
│ User Signs Up (Registration)        │
│ ├─ Creates new User                 │
│ ├─ Creates new Client               │
│ ├─ Creates ClientUser (admin)       │
│ └─ Auto-creates Trial Subscription  │
└─────────────────────────────────────┘
              ↓
      status: 'trial'
      plan: 'trial'
      14-day countdown starts
              ↓
    ┌─────────────────────┐
    │ Trial Active        │
    │ ├─ Can access basic │
    │ │   features        │
    │ ├─ 14 days remain   │
    │ └─ No payment yet   │
    └─────────────────────┘
              ↓ (User upgrades)
    ┌─────────────────────────────────┐
    │ POST /subscription/activate     │
    │ {                               │
    │   plan_id: 'professional',      │
    │   billing_cycle: 'monthly'      │
    │ }                               │
    └─────────────────────────────────┘
              ↓
    ┌─────────────────────────────────┐
    │ Razorpay Webhook (TODO)         │
    │ ├─ Create checkout link         │
    │ ├─ User completes payment       │
    │ └─ Verify payment               │
    └─────────────────────────────────┘
              ↓
    ┌─────────────────────────────────┐
    │ Update Subscription             │
    │ ├─ status: 'active'             │
    │ ├─ plan: 'professional'         │
    │ ├─ billing cycle: 'monthly'     │
    │ ├─ period_start: today          │
    │ ├─ period_end: today + 30 days  │
    │ ├─ renewal_date: auto           │
    │ └─ Create Invoice               │
    └─────────────────────────────────┘
              ↓
    ┌─────────────────────┐
    │ Active Subscription │
    │ ├─ Features enabled │
    │ │  based on plan    │
    │ ├─ Usage tracked    │
    │ │  per period       │
    │ └─ Auto-renewal set │
    └─────────────────────┘
              ↓
        (Multiple paths)
        ↙              ↘
   RENEW (auto)    CANCEL (user request)
   ↓               ↓
   Period end  POST /subscription/cancel
   ↓               ↓
   Charge again  status: 'cancelled'
   ↓             cancellation_reason: '...'
   New period   immediate: true|false
   ↓               ↓
   Continue        ├─ False: End at period end
   subscription    └─ True: Immediate cancellation
                   ↓
                Features disabled
```

## 📊 Data Model Relationships

```
User (1)
  └──→ (Many) ClientUser
  │           ├──→ Client (Many)
  │           ├──→ Role (Optional)
  │           └──→ [permissions]
  │
  ├──→ (Many) LoginHistory
  ├──→ (1) google_id (Optional, OAuth)
  └──→ (Many) created_by references
              (in Campaign, Template, etc)

Client (1)
  ├──→ (Many) ClientUser
  │           └──→ User
  │
  ├──→ (Many) Subscription (1 active at a time)
  │           └──→ Plan
  │               └──→ PlanFeature
  │                   └──→ Feature
  │
  ├──→ (Many) Campaign
  ├──→ (Many) Template
  ├──→ (Many) Lead
  ├──→ (Many) Workflow
  │           └──→ n8n_workflow_id
  │
  ├──→ (Many) FacebookPageConnection
  │           └──→ (1 to Many) FacebookLeadForm
  │
  ├──→ (Many) AuditLog
  ├──→ (Many) Invoice
  ├──→ (Many) NetworkConnection
  └──→ (1) BrandingSetting

Plan (1)
  └──→ (Many) PlanFeature
      ├──→ Feature (Many)
      └──→ [limits]
          ├─ max_contacts
          ├─ max_campaigns
          ├─ max_templates
          └─ ...

Subscription (1)
  ├──→ Plan
  ├──→ (Many) SubscriptionUsage
  │           └──→ Feature
  │
  ├──→ (Many) Invoice
  │           └──→ (Optional) Payment
  │
  └──→ Client
```

## 🔄 Feature Gate Check Flow

```
Request to protected endpoint
(e.g., POST /api/leads)
      ↓
@requireFeature('leads') middleware
      ↓
    ┌────────────────────────────────┐
    │ Is user Super Admin?           │
    └────────────────────────────────┘
         ↙         ↘
       YES         NO
        ↓          ↓
     ALLOW    ┌────────────────────────┐
              │ Check Feature Enabled   │
              │ in Subscription Plan    │
              └────────────────────────┘
                      ↓
              ┌────────────────────────┐
              │ req.features['leads']? │
              └────────────────────────┘
                  ↙         ↘
                YES         NO
                 ↓          ↓
              ALLOW    RETURN 403
                       "Your plan doesn't
                        include this
                        feature"
                       
                       If needed for upgrade:
                       → Show tier requirements
                       → Link to upgrade
```

## 📈 Usage Limit Check Flow

```
Action that consumes quota
(e.g., Create new Lead)
      ↓
@checkUsageLimit('leads', 'max_contacts')
      ↓
    ┌──────────────────────────────┐
    │ Is Super Admin?              │
    │ Is subscription active?      │
    │ Is plan limit defined?       │
    └──────────────────────────────┘
           ↓
    ┌──────────────────────────────┐
    │ Get current billing period   │
    │ period_start → period_end    │
    └──────────────────────────────┘
           ↓
    ┌──────────────────────────────┐
    │ Query SubscriptionUsage      │
    │ WHERE subscription_id        │
    │ AND feature_code             │
    │ AND period matches           │
    └──────────────────────────────┘
           ↓
    ┌──────────────────────────────┐
    │ Get current usage count      │
    │ (usage.usage_count)          │
    └──────────────────────────────┘
           ↓
    ┌──────────────────────────────┐
    │ Compare with limit           │
    │ current < limit ?            │
    └──────────────────────────────┘
      ↙         ↘
     YES         NO
      ↓          ↓
   ALLOW    RETURN 403
            "Limit reached:
             You have X/Y
             contacts.
             Upgrade to
             increase limit."
   
   Then increment usage by 1
```

## 🔄 Webhook Handler Flow

```
External Service (Facebook, Razorpay, etc)
sends webhook POST request
      ↓
┌───────────────────────────────────┐
│ 1. Receive raw webhook body       │
└───────────────────────────────────┘
      ↓
┌───────────────────────────────────┐
│ 2. Verify signature               │
│    (HMAC-SHA256 expected)         │
│    ├── Extract from header        │
│    ├── Recompute using secret     │
│    └── Compare                    │
└───────────────────────────────────┘
      ↓
      ├─ INVALID? → 401 Unauthorized
      │
      └─ VALID? Continue...
             ↓
┌───────────────────────────────────┐
│ 3. Parse webhook payload          │
│    ├── Extract event type         │
│    ├── Extract resource data      │
│    └── Validate schema            │
└───────────────────────────────────┘
      ↓
┌───────────────────────────────────┐
│ 4. Route by event type            │
│    ├── facebook.lead_received     │
│    ├── razorpay.payment.success   │
│    ├── razorpay.payment.failed    │
│    └── etc                        │
└───────────────────────────────────┘
      ↓
    Route-specific handler
    ├─ Facebook Lead
    │  ├─ Create Lead in DB
    │  ├─ Assign to client
    │  └─ Log to AuditLog
    │
    └─ Razorpay Payment
       ├─ Verify amount matches
       ├─ Update Subscription
       ├─ Create Invoice
       └─ Send Receipt Email (TODO)
      ↓
┌───────────────────────────────────┐
│ 5. Return 200 OK                  │
│    (Webhook acknowledged)         │
└───────────────────────────────────┘
      ↓
    Process async if needed
    ├─ Queue task in Bull (TODO)
    ├─ Send notifications
    └─ Update related records
```

## 🚀 Deployment Architecture

```
Developer Workstation
      ↓
     Git Commit → GitHub/GitLab
              ↓
          Coolify detects push
              ↓
    ┌────────────────────────────┐
    │ Docker Build Process       │
    ├────────────────────────────┤
    │ Backend Dockerfile:        │
    │ ├─ FROM node:18            │
    │ ├─ Installs dependencies   │
    │ ├─ Builds application      │
    │ └─ Exposes port 5000       │
    │                            │
    │ Frontend Dockerfile:       │
    │ ├─ Build with Next.js      │
    │ ├─ Standalone output       │
    │ └─ Exposes port 3000       │
    └────────────────────────────┘
              ↓
    ┌────────────────────────────┐
    │ Docker Compose Orchestration
    ├────────────────────────────┤
    │ ├─ Backend Container       │
    │ │  └─ Linked to MongoDB    │
    │ ├─ Frontend Container      │
    │ │  └─ Linked to Backend    │
    │ └─ Network Configuration   │
    └────────────────────────────┘
              ↓
    ┌────────────────────────────────┐
    │ Coolify Platform               │
    ├────────────────────────────────┤
    │ ├─ Domain Mapping              │
    │ │  ├─ app.yourdomain.com →     │
    │ │  │  Frontend (3000)          │
    │ │  └─ api.yourdomain.com →     │
    │ │     Backend (5000)           │
    │ ├─ SSL Certificate            │
    │ │  └─ Auto-renewal           │
    │ ├─ Environment Variables      │
    │ └─ Health Checks              │
    └────────────────────────────────┘
              ↓
    ┌────────────────────────────────┐
    │ MongoDB (External Instance)    │
    │ 72.61.248.175:5443            │
    └────────────────────────────────┘
              ↓
    ┌────────────────────────────────┐
    │ Users Access Application       │
    │ ├─ Visit app.yourdomain.com    │
    │ ├─ Frontend served via CDN/    │
    │ │  Nginx                       │
    │ └─ API calls to backend        │
    │    via api.yourdomain.com      │
    └────────────────────────────────┘
```

---

## 📋 Key Sequences at a Glance

### Login Sequence (4 database queries)
```
1. SELECT User WHERE email
2. SELECT ClientUser WHERE user_id & client_id  
3. SELECT Client WHERE _id
4. SELECT Subscription & Plan data...
Result: JWT tokens + user context
```

### Feature Gate Check (3 queries)
```
1. SELECT Feature WHERE code ✓ (enabled?)
2. Check req.features[code] ✓ (in plan?)
3. Check req.featureLimits[code] ✓ (usage?)
Result: ALLOW or DENY
```

### Create Lead (2 writes)
```
1. INSERT Lead document
2. INCREMENT SubscriptionUsage
Result: Lead created & usage tracked
```

---

**Visual Architecture Reference**  
**Last Updated**: February 16, 2026
